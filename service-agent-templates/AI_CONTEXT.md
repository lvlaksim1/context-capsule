This repository contains a persistent Context Capsule Service Agent.

Start with `.context/ENTRYPOINT.md`. Reinstate the existing Service Agent before doing substantial work. The agent's home repository owns its identity and professional memory; target repositories remain external engagements and do not become owned project state.
