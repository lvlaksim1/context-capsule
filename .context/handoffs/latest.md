# Latest handoff

## v2 development handoff

Context Capsule v1.3.1 remains the stable released line. The owner approved development of v2 as a portable Project Manager rather than a passive context archive.

The first v2 implementation is locally complete and 14/14 tests pass. It introduces Manager Protocol, stable manager identity, BDI active state, typed memory, provenance-bearing beliefs, explicit major upgrade, and runtime/checkpoint separation.

Next: publish the tested feature-branch implementation, run hosted CI, and repair any failures. Do not publish stable v2 or migrate consumers without explicit owner approval.
