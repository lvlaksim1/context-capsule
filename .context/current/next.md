# Next actions

1. Publish v1.2.0 and verify CI/self-validation.
2. Use `fgis-fsa-il` as the first rich-legacy adoption test.
3. Use `telegram-receiver` as the compact legacy adoption test.
4. Use `ai-agent-lab` as the branch-redirect + `.agent/` semantic-sync adoption test.
5. After those production adoptions, refine Core only from observed evidence rather than hypothetical complexity.
