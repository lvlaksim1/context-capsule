# Context history

Keep compact historical records needed to explain how the current project state was reached.

Use `dialogues/` for evidence-rich investigation episodes and `decisions/` for durable decisions. This directory is for other historical context that remains useful but is not current state.
