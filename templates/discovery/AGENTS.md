## Context Capsule discovery

Before substantial work, read .context/ENTRYPOINT.md.

The authoritative durable context is on {{AUTHORITATIVE_BRANCH}}. Restore it there before analysis or mutation.

During substantial work, persist durable semantic changes back to the authoritative context branch; feature branches are live-work locations, not Context Capsule authority.
